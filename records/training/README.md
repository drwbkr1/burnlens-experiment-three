# Frozen training records

Milestone 4 machine-readable public receipts belong here. Benchmark arrays,
model weights, probabilities, histories containing raw values, and run packages
remain in controlled external custody under `C:\Projects\Active` and are not
committed to Git.

At Milestone 4 entry there are zero training runs and zero checkpoints.

The current locally verified candidate records three frozen training runs,
three selected checkpoints, three validation-only inference packages, exact
replay, and zero evaluations. External attempts 001 through 004 remain retained
as failed or invalid; attempt 005 is the sole protocol-conformant candidate.
