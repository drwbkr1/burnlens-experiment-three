# Devlog: Milestone 1 Controlled Intake Candidate

| Field | Value |
| --- | --- |
| Date | 2026-08-24 |
| Milestone | 1 |
| Tracker | [Issue #2](https://github.com/drwbkr1/burnlens-experiment-three/issues/2) |
| Review | [Draft PR #3](https://github.com/drwbkr1/burnlens-experiment-three/pull/3) |

## Outcome first

The exact Experiment One retrospective benchmark is now ready for Experiment
Three's later neural lifecycle. One attested owner/rightsholder `yes` was locked
and reconciled, a current six-source gate passed, dataset readiness passed, and
131 approved artifacts totaling 3,369,748 bytes were copied without replacement
into isolated external custody. Every source, staged, and promoted identity
matched. No benchmark byte or raw human-review material was added to Git.

This is not a model result. Experiment Three still has zero model
implementations, training runs, checkpoints, inference runs, evaluations,
metrics, renders, and releases.

## What was admitted

The admitted package binds the 48-array dataset, whole-event split, masks,
normalization, evaluation definitions, RBR and constant-control records,
canonical U-Net weights and predictions, source and terms lineage, and selected
review renders. The receipt enumerates all 131 assets and binds the destination
roster at SHA-256
`0daf93b2b3a21330d501c9e222d907738c19e4d5b9e00ebbdd169b65aadb89f4`.

The admission deliberately excludes native provider bytes, restricted Tepee
BARC4/BARC256 material, the optimizer-bearing and nonselected checkpoints, the
current U-Net source as a substitute for the historical implementation, private
review material, credentials, Experiment Two/Two-B artifacts, and fresh-event
evidence. Repository redistribution was not authorized.

## Retained failures

The first focused source-test attempt used system Python: six split tests passed
and three modules failed to import because `rasterio` was absent. The same 25
focused tests passed in Experiment One's locked environment. This is retained
as runtime-selection evidence, not hidden as a clean first attempt.

Intake attempt 001 promoted 48 assets, staged the next asset, then stopped when
a structural checker used the `.part` suffix and tried to decode a PNG as text.
No collision, overwrite, or mismatch occurred. The resume contract reverified
the first 48 promotions and promoted the remaining 83. The final receipt
retains both attempts.

## Claim boundary and next checkpoint

This remains a retrospective sparse-core compatibility benchmark whose test
results were already known. It cannot establish independent accuracy, dense
segmentation, generalization, significance, superiority, or operational
wildfire usefulness. The next checkpoint is integrated local verification,
followed by required PR checks, reviewed merge, and direct live-main
verification. Model work cannot begin before that acceptance.
