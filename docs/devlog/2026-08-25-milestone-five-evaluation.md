# Milestone 5: controlled retrospective evaluation

Milestone 5 began from the accepted Milestone 4 checkpoint, not from a new
model idea. The exact three seed checkpoints and validation-selected threshold
`0.5` remained fixed.

Issue #10, profile 006, the M5 milestone contract, and state reconciliation 006
establish a staged one-opening path. First comes a metadata-only identity and
rights audit. Next, the full metric, geospatial, render, packaging, and verifier
path must pass using fabricated fixtures. Only then may one bounded invocation
open the known test for all three seeds, RBR, the canonical U-Net, and both
constant controls.

Both prerequisite stages passed. `M5-OPENING-2026-001` then opened the known
test exactly once for all required methods. The lifecycle completed and replayed
exactly, so lifecycle status is `PASS`. The three-seed median did not beat the
strongest constant control and one seed collapsed on one event, so the frozen
comparative status is `FAIL`. Seed `20260727` performed much better, but it was
not substituted after test observation.

The evaluation remains retrospective and sparse-core bounded. No test-driven
tuning, omitted seed, rescue threshold, new architecture, fresh cohort, tag, or
release occurred. The next step is reviewed publication and live verification
of this exact candidate.
